just random stuff.

this repo is a dump for small things i build while learning or messing around.
some are ai-assisted, some are manual, most are not serious.

examples:
- browser extensions
- tiny backend experiments
- parsers, scripts, prototypes
- “what if i try this” code

nothing here is production ready.
if something grows big, it’ll move to its own repo.

structure will change.
things may break.
that’s fine.


stuff:

| name       | what it is? |
|------------|------|
| GPTcontext | prompt context injection experiment - extension |
| asynq | background task queue with worker - using asynq - Golang |
| Sxrapie | Auto-capture LinkedIn profiles stealthily - extension |
| Ghost Scraper (Company) | Auto-capture LinkedIn Company pages stealthily - extension |
| Twitter Vibe Check AI | Vibe check/cleaner for Twitter using AI - extension |
| ExcaliVault | Save and manage ExcaliDraw canvas snapshots as projects - extension |
| Copy0 | Keyboard-driven session clipboard manager with long-press shortcuts - extension |
| Env Masker | Visually hides .env values while keeping keys visible - VS Code extension |
| js-context-retrieval | searches relevancy js with query - return top bottom |
| Lattice | Framework for extracting context/parsing multiple languages (JS, TS, PY, GO) |
| bm25 | BM25 ranking algorithm implementation for text search |

